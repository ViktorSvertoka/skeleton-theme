{% if size_guide and size_guide.headers != blank and size_guide.rows != blank %}
  {% assign size_guide_title = size_guide_title | default: 'Size Guide' %}
  {% assign size_guide_id = 'size-guide-' | append: section_id %}
  <div
    class="fixed inset-0 z-50 hidden items-center justify-center p-4"
    data-size-guide
    aria-hidden="true"
  >
    <div
      class="absolute inset-0 bg-black/60"
      data-size-guide-overlay
      tabindex="-1"
    ></div>
    <div
      class="relative w-full max-w-xl rounded-2xl bg-white p-7 shadow-[0_20px_45px_rgba(17,17,17,0.2)]"
      role="dialog"
      aria-modal="true"
      aria-labelledby="{{ size_guide_id }}"
      tabindex="-1"
      data-size-guide-dialog
    >
      <div class="mb-4 flex items-center justify-between gap-4">
        <h3 id="{{ size_guide_id }}" class="text-xl font-semibold text-dark">{{ size_guide_title }}</h3>
        <button
          type="button"
          class="inline-flex h-9 w-9 items-center justify-center "
          data-size-guide-close
          aria-label="Close size guide"
        >
          <svg class="h-6 w-6 rotate-45" aria-hidden="true">
            <use href="{{ 'sprite.svg' | asset_url }}#plus"></use>
          </svg>
        </button>
      </div>

      <div class="overflow-x-auto">
        <table class="w-full border-collapse text-sm">
          <tbody>
            {% for header in size_guide.headers %}
              {% assign row_index = forloop.index0 %}
              {% assign row_values = size_guide.rows[row_index] %}
              <tr class="{% cycle 'bg-white', 'bg-gray-50' %}">
                <th scope="row" class="px-4 py-3 text-left text-sm font-semibold text-dark">
                  {{ header }}
                </th>
                {% for value in row_values %}
                  <td class="px-4 py-3 text-center text-sm text-dark">
                    {{ value }}
                  </td>
                {% endfor %}
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  </div>
{% endif %}
